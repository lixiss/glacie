using Glacie.Modules;

namespace Glacie
{
    // TODO: Should it be ProjectContext, which may expose Database directly?
    public sealed class Context
    {
        public Context(TargetModule targetModule, SourceModule[] sourceModules)
        {
        }
    }
}
